You are a SQL Server expert. Optimize this stored procedure.

PROCEDURE: SearchAllTables

SQL CODE:
```sql

CREATE PROC [dbo].[SearchAllTables]    --exec[dbo].[SearchAllTables]'202009' search all database
(
    @SearchStr nvarchar(100)
)
AS
BEGIN

-- Copyright © 2002 Narayana Vyas Kondreddi. All rights reserved.
-- Purpose: To search all columns of all tables for a given search string
-- Written by: Narayana Vyas Kondreddi
-- Site: http://vyaskn.tripod.com
-- Tested on: SQL Server 7.0 and SQL Server 2000
-- Date modified: 28th July 2002 22:50 GMT

DECLARE @Results TABLE(ColumnName nvarchar(370), ColumnValue nvarchar(3630))

SET NOCOUNT ON

DECLARE @TableName nvarchar(256), @ColumnName nvarchar(128), @SearchStr2 nvarchar(110)
SET  @TableName = ''
SET @SearchStr2 = QUOTENAME('%' + @SearchStr + '%','''')

WHILE @TableName IS NOT NULL
BEGIN
... (truncated) ...
  BEGIN
            INSERT INTO @Results
            EXEC
            (
                'SELECT ''' + @TableName + '.' + @ColumnName + ''', LEFT(' + @ColumnName + ', 3630) 
                FROM ' + @TableName + ' (NOLOCK) ' +
                ' WHERE ' + @ColumnName + ' LIKE ' + @SearchStr2
            )
        END
    END 
END

SELECT ColumnName, ColumnValue FROM @Results
END




```

STATS:
📊 Execution: 7 | ⏱️ Duration: 112753ms | 💻 CPU: 101014ms | 📖 Reads: 60262400

PLAN:
── PLAN D'EXÉCUTION (points chauds) ──
Opérateurs coûteux :
  • Compute Scalar sur [sysschobjs] — coût 0.2633 — ~1 lignes
  • Stream Aggregate sur [sysschobjs] — coût 0.2633 — ~1 lignes
  • Filter sur [sysschobjs] — coût 0.2633 — ~18.7461 lignes
  • Compute Scalar sur [sysschobjs] — coût 0.2628 — ~342.256 lignes
  • Nested Loops sur [sysschobjs] — coût 0.2627 — ~342.256 lignes
Lignes estimées (racine) : 1.0
INDEXES:
No indexes found

BUSINESS RULES:
1. 1. Correctness & Maintainability Rules BP-001
2. 2. Query Performance Rules BP-010
3. 3. Transactions & Concurrency Rules BP-030
4. 4. Data Type Rules BP-040
5. 5. Monitoring & Maintenance Rules BP-050
6. 6. Rules for LLM-Generated or Automatically Rewritten SQL The rules below apply 

RULES TO RESPECT :
1. Never convert LEFT JOIN to INNER JOIN - this would change results
2. Avoid SELECT * - prefer explicit column list
3. Do not use functions on columns in WHERE


IMPORTANT INSTRUCTIONS:
- Keep the SAME functionality as the original procedure
- Do not invent tables, columns, or objects that do not exist
- Preserve the original parameter names and data types
- If you cannot safely optimize, say so clearly in the diagnostic
- Do not reduce the scope of the procedure
- Only suggest changes that preserve the exact same results

Reply ONLY in JSON:
{
    "diagnostic": "problem description",
    "code_optimise": "CREATE PROCEDURE ...",
    "explication": "why it works",
    "gain_estime": 75,
    "risque": "LOW"
}
Risk: LOW/MEDIUM/HIGH | Gain: 0-100